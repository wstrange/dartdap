# LDAP configuration file

connections:
  test-LDAP:
    host: localhost
    baseDN: dc=example,dc=com
    port: 1389
    bindDN: "cn=Directory Manager"
    password: password
    ssl: false

  test-LDAPS:
    host: localhost
    ssl: true
    baseDN: dc=example,dc=com
    port: 1636
    bindDN: "cn=Directory Manager"
    password: password

  opendj:
    port: 1389
    host: localhost
    baseDN: "dc=example,dc=com"
    bindDN: "cn=Directory Manager"
    password: password