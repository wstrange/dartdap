# LDAP configuration file

connections:
  test-LDAP:
    host: localhost
    baseDN: o=userstore
    #  port: 10389
    #  bindDN: cn=Manager,dc=example,dc=com
    #  password: p@ssw0rd
    port: 1389
    bindDN: "cn=Directory Manager"
    password: password

  test-LDAPS:
    host: localhost
    ssl: true
    baseDN: o=userstore
    #  port: 10636
    #  bindDN: cn=Manager,dc=example,dc=com
    #  password: p@ssw0rd
    port: 1389
    bindDN: "cn=Directory Manager"
    password: password

  opendj:
    port: 1389
    host: localhost
    baseDN: "o=userstore"
    bindDN: "cn=Directory Manager"
    password: password